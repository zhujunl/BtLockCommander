package com.miaxis.btlockcommanderdemo.model.local.greenDao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.miaxis.btlockcommanderdemo.model.entity.BtDevice;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "BT_DEVICE".
*/
public class BtDeviceDao extends AbstractDao<BtDevice, Long> {

    public static final String TABLENAME = "BT_DEVICE";

    /**
     * Properties of entity BtDevice.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Mac = new Property(2, String.class, "mac", false, "MAC");
        public final static Property SerialNumber = new Property(3, String.class, "serialNumber", false, "SERIAL_NUMBER");
        public final static Property Keeper = new Property(4, String.class, "keeper", false, "KEEPER");
    }


    public BtDeviceDao(DaoConfig config) {
        super(config);
    }
    
    public BtDeviceDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"BT_DEVICE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"MAC\" TEXT UNIQUE ," + // 2: mac
                "\"SERIAL_NUMBER\" TEXT UNIQUE ," + // 3: serialNumber
                "\"KEEPER\" TEXT);"); // 4: keeper
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"BT_DEVICE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, BtDevice entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String mac = entity.getMac();
        if (mac != null) {
            stmt.bindString(3, mac);
        }
 
        String serialNumber = entity.getSerialNumber();
        if (serialNumber != null) {
            stmt.bindString(4, serialNumber);
        }
 
        String keeper = entity.getKeeper();
        if (keeper != null) {
            stmt.bindString(5, keeper);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, BtDevice entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String mac = entity.getMac();
        if (mac != null) {
            stmt.bindString(3, mac);
        }
 
        String serialNumber = entity.getSerialNumber();
        if (serialNumber != null) {
            stmt.bindString(4, serialNumber);
        }
 
        String keeper = entity.getKeeper();
        if (keeper != null) {
            stmt.bindString(5, keeper);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public BtDevice readEntity(Cursor cursor, int offset) {
        BtDevice entity = new BtDevice( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // mac
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // serialNumber
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // keeper
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, BtDevice entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setMac(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSerialNumber(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setKeeper(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(BtDevice entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(BtDevice entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(BtDevice entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
